import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'OumaimaHraichi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
